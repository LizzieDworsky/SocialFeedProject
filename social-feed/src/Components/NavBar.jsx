import React from "react";

const NavBar = () => {
    return (
        <header>
            <h1>
                Social <span className="feed">Feed</span>
            </h1>
        </header>
    );
};

export default NavBar;
